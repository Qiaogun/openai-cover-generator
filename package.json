{
  "name": "openai-cover-generator",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy .",
    "deploy:workers": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.65.0"
  }
}
